{
  "name": "cypherox-eleventy",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "scss": "sass src/assets/scss/main.scss src/assets/css/style.css --style=compressed",
    "watch:scss": "sass --watch src/assets/scss/main.scss:src/assets/css/style.css --style=compressed",
    "serve": "npx @11ty/eleventy --serve",
    "start": "npm-run-all --parallel watch:scss serve",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.82.0"
  },
  "dependencies": {
    
  }
}
